{
  "name": "cognito-serverless-app",
  "version": "1.0.0",
  "description": "Serverless web application with Cognito authentication",
  "main": "index.js",
  "scripts": {
    "deploy": "./deploy.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless": "^3.30.1",
    "aws-sdk": "^2.1395.0"
  },
  "devDependencies": {
    "serverless-offline": "^14.4.0"
  }
}
